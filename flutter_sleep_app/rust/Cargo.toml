[package]
name = "sleep_audio_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
flutter_rust_bridge = "2.0"
rand = "0.8"
hound = "3.5"  # For WAV file writing

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
